import styled from "styled-components";

export const MenuOption = styled.a`
  opacity: 0.8;
  font-size: 2rem;
  margin: 0 1rem;
  cursor: pointer;
  transition: opacity 0.3s linear;
  &:hover {
    opacity: 1;
  }
`;
